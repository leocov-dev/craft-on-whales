// Shared types for the inventory module — re-exported from shared/types so
// frontend and backend use the literal same interfaces.
export type {
  NormalizedEnchant,
  NormalizedItem,
  NestedInventoryEntry,
  NestedInventory,
} from '../../../shared/types/inventory';
